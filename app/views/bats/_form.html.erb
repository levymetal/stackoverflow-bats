<%= form_for(@bat) do |f| %>
  <% if @bat.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@bat.errors.count, "error") %> prohibited this bat from being saved:</h2>

      <ul>
      <% @bat.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :manufacturer_id %><br>
    <%= f.collection_select :manufacturer_id, Manufacturer.all, :id, :name  %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
